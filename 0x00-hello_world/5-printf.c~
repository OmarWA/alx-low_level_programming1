#include <stdio.h>

/**
 * main -- The entry point of the whole program
 * Return:
 * 0 if everything went okay, non-zero otherwise.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
