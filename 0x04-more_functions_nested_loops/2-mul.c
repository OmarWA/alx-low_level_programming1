#include "main.h"
/**
 * mul - multiplies two given integers and returns the product
 * @a: first integer
 * @b: second integer
 * Return: the product of a times b
 */
int mul(int a, int b)
	return ((a * b));
